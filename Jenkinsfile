pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
    }
    stages {
        stage('Deploy') {
            steps {
                sh 'echo RamySabryStart_1
                sh 'echo env.GIT_BRANCH'
                sh 'echo RamySabryEnd_1
            }
        }
    }
}
